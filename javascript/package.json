{
    "name": "root",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "build": "pnpm --recursive run build",
        "prettier": "prettier --write .",
        "test": "vitest --run"
    },
    "devDependencies": {
        "@vitest/coverage-v8": "^3",
        "prettier": "^3",
        "vitest": "catalog:"
    }
}
