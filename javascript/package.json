{
    "name": "root",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "build": "pnpm --recursive run build",
        "prettier": "prettier --write 'packages/@xstache/*/*.json' 'packages/@xstache/*/src/**.{ts,tsx}'",
        "test": "pnpm --recursive run test"
    },
    "devDependencies": {
        "prettier": "^3"
    }
}
